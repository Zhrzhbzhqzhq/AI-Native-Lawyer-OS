# M136.3 Empty Documents Source Audit

Audit time: 2026-07-09
Database: postgresql://qingzhang@localhost:5432/lawdesk
Target: empty documents (content and content_uri both empty)

Summary of exports:
- docs/audit/M136.3_empty_documents_by_matter.csv (467 rows)
- docs/audit/M136.3_empty_documents_by_title.csv (30 rows)
- docs/audit/M136.3_empty_documents_target_matter.csv (3 rows)

SQL (read-only) used (summaries):
- By matter:
  SELECT matter_id, count(*) AS cnt FROM documents WHERE (content IS NULL OR trim(content)='') AND (content_uri IS NULL OR trim(content_uri)='') GROUP BY matter_id ORDER BY cnt DESC
- By title:
  SELECT title, count(*) AS cnt FROM documents WHERE (content IS NULL OR trim(content)='') AND (content_uri IS NULL OR trim(content_uri)='') GROUP BY title ORDER BY cnt DESC
- By document_type:
  SELECT document_type AS type, count(*) FROM documents WHERE (content IS NULL OR trim(content)='') AND (content_uri IS NULL OR trim(content_uri)='') GROUP BY document_type ORDER BY count DESC
- By created date:
  SELECT to_char(created_at::date,'YYYY-MM-DD') AS day, count(*) FROM documents WHERE (content IS NULL OR trim(content)='') AND (content_uri IS NULL OR trim(content_uri)='') GROUP BY day ORDER BY day
- Target matter rows:
  SELECT id, document_id, matter_id, title, document_type AS type, status, created_at, updated_at FROM documents WHERE (content IS NULL OR trim(content)='') AND (content_uri IS NULL OR trim(content_uri)='') AND matter_id='m-mrco9m97-dbygsz'

Key statistics:
- Total empty documents: 908
- Distinct matter groups with empty docs (rows in by_matter CSV): 467
- Distinct titles among empty docs (rows in by_title CSV): 30
- Empty docs in target matter `m-mrco9m97-dbygsz`: 3

Top matter_id distribution (top 20):
( matter_id | count )
- m-mrcdpaea-gdj049 | 13
- test-matter-ci-... (various) | 6 each (many test-matter-ci-... entries)
- ... (see docs/audit/M136.3_empty_documents_by_matter.csv for full list)

Top titles (top 20):
- 代理词（更新） | 134
- op.pdf | 74
- challenge doc | 72
- Original Doc | 68
- 庭审提纲（更新） | 68
- t | 66
- Document 1..6, Doc 1 | 57..51
- Draft preservation notice | 8
- heading/paragraph/field/item | small counts

Document type distribution:
- (empty string) | 353
- challenge_opinion | 212
- representation | 202
- hearing_outline | 68
- contract | 50
- draft | 19
- 起诉状 | 1
- 诉讼策略建议 | 1
- raw | 1
- 证据清单 | 1

Date distribution (created_at):
- 2026-07-03: 11
- 2026-07-04: 496
- 2026-07-05: 378
- 2026-07-08: 23

Observations & initial judgment:
1. Time pattern: the bulk of empty documents (496 + 378 = 874) were created on 2026-07-04 and 2026-07-05, with a few on 2026-07-03 and 2026-07-08. This suggests a concentrated ingestion or automated process activity on those dates rather than sporadic user behavior.
2. Matter ids show many `test-matter-ci-*` and `mock-intake-*` naming patterns, and high counts for `m-mrcdpaea-gdj049`. This suggests test data or automated CI/import flows contributed a substantial portion.
3. Titles include many templated names like `代理词（更新）`, `op.pdf`, `Original Doc`, and numbered `Document 1..6`, which aligns with bulk import/ingest or test fixture naming. The presence of `op.pdf` and `Original Doc` might indicate original files referenced but not yet processed to content_uri.
4. Document types: a large share have empty `document_type` (353) and many `challenge_opinion`/`representation` types—consistent with legal document imports or template generation.
5. Target matter `m-mrco9m97-dbygsz` has 3 empty documents; created_at and other fields are in `docs/audit/M136.3_empty_documents_target_matter.csv` for inspection.

Probable causes (ranked):
- Most likely: Import / ingestion process created placeholders without content (bulk): matches date spike and test/CI matter names. (High probability)
- Possible: Test data / CI artifacts left in DB (naming like `test-matter-ci-*`): also likely. (High probability)
- Less likely: Normal business placeholders — possible but unlikely for the large spike unless a mass user action occurred. (Low probability)
- Document structure fragments: possible for small fraction, but overall pattern points to import/test artifacts. (Medium-Low)

Recommendations (non-destructive next steps):
1. Review import/ingest logs around 2026-07-04 to 2026-07-05 (system logs, AI runtime logs, intake service logs) to identify process that created many empty docs.
2. For sample matters (top matter ids in CSV), inspect the matter context to see whether these were test matters. Use UI or API to check matter titles/owners.
3. Provide CSVs to product/ops for business decision (already exported in `docs/audit`).
4. If confirmed test/CI data, schedule a safe cleanup (backup + delete) outside this audit.

Files created:
- docs/audit/M136.3_empty_documents_by_matter.csv
- docs/audit/M136.3_empty_documents_by_title.csv
- docs/audit/M136.3_empty_documents_target_matter.csv
- docs/audit/M136.3_empty_documents_source_audit.md

Next actions I can perform (on request):
- Export full details (all 908 rows) with additional fields (creator, source) if available.
- Correlate `created_at` spikes with application logs (requires log access).

---
Audit performed read-only by querying the database and exporting CSVs; no data or schema modifications were made.
