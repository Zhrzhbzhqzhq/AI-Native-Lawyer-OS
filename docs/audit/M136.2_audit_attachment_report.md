# M136.2 Audit Attachment Report

Export time: 2026-07-09T

Database: postgresql://qingzhang@localhost:5432/lawdesk
Target matter: m-mrco9m97-dbygsz

Files exported:
- docs/audit/M136.2_empty_documents.csv
- docs/audit/M136.2_duplicate_documents.csv

SQL (summary of the read-only queries used):

1) Empty documents export SQL:
```
SELECT id, document_id, matter_id, title, document_type AS type, status, created_at, updated_at
FROM documents
WHERE (content IS NULL OR trim(content)='')
  AND (content_uri IS NULL OR trim(content_uri)='')
```
Exported with psql \copy to CSV.

2) Duplicate documents export SQL (by matter_id+title):
```
SELECT d.id, d.document_id, d.matter_id, d.title, d.document_type AS type, d.status, d.created_at, d.updated_at, d.content, d.content_uri
FROM documents d
JOIN (SELECT matter_id, title FROM documents GROUP BY matter_id, title HAVING COUNT(*)>1) dup
  ON d.matter_id = dup.matter_id AND d.title = dup.title
ORDER BY d.matter_id, d.title
```
Exported with psql \copy to CSV.

Export results (counts):
- Empty documents exported: 908
- Duplicate groups (matter_id+title): 4
- Duplicate records exported: 10
- Empty documents involving target matter `m-mrco9m97-dbygsz`: 3
- Duplicate records involving target matter `m-mrco9m97-dbygsz`: 0

Initial judgment:
- The empty documents (908 rows) likely indicate bulk ingestion/test data or incomplete creations. They affect UX and storage; require triage.
- Duplicate document groups are few (4 groups, 10 records) and should be inspected for accidental duplicates vs intentional versions.

Next steps (recommended, non-destructive):
1. Business triage: export the 908 rows (done) and review creators/timestamps/source to determine origin.
2. For duplicates, compare content/timestamps and decide dedupe strategy or retention policy.
3. Add application-level validation to prevent creating documents without content or content_uri if business requires.
4. Consider adding periodic integrity checks/alerts.

Files created in repo (read-only exports):
- docs/audit/M136.2_empty_documents.csv
- docs/audit/M136.2_duplicate_documents.csv
- docs/audit/M136.2_audit_attachment_report.md

Notes:
- This audit was read-only: no schema changes, no data modifications, no migrations applied.
- If you want full lists or additional CSV formats (e.g., including all fields or JSON export), I can produce them on request.
