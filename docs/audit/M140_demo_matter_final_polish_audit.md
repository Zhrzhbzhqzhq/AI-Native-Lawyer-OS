# M140 Demo Matter Final Polish Audit

Date: 2026-07-10

Scope
- Target matter: `m-mrco9m97-dbygsz` (demo candidate). 
- Read-only audit: no code, schema, or data changes.
- Checks: Matter Overview, Evidence, Facts, Issues, Laws, Arguments, Documents; presence of mock/demo text; UI rendering anomalies.

Summary (brief)
- Matter `m-mrco9m97-dbygsz` is a real intake titled **M122 Material Evidence Validation** with a meaningful description describing a RMB 100,000 loan dispute. Overview page renders successfully.
- Backend API counts and content (snapshot saved under `/tmp/m140_*.json`) show: documents=3, evidence=8 (or more), facts≈13, issues≈6, laws≈13, arguments≈11 — resources populated by AI intake and look domain-relevant (not mock/demo copy).
- Documents: all 3 documents are drafts with `content = null` and `content_uri = ""` (empty drafts) — historical AI-created placeholders. They are present in the Documents listing (page rendered) and will appear in the UI unless filtered/archived.
 - Evidence page: M141 已修复 `selectedEvidence` null guard；runtime 验证通过，页面返回 HTTP 200；后端 API 证据数量为 8；当前不再阻断演示。
- Facts / Issues / Laws / Arguments pages rendered successfully and show meaningful content.

Detailed Findings

1) Matter Overview
- API: `/matters/m-mrco9m97-dbygsz` returned a populated object (title, description, status active).
- Frontend: `GET /matters/m-mrco9m97-dbygsz` returned HTTP 200 and page HTML saved to `/tmp/m140_overview.html`.
- Suitability: Overview content is concise and demo-appropriate. No mock/test strings observed in the Overview object.

2) Evidence / Facts / Issues / Laws / Arguments
- API snapshots (saved to `/tmp/m140_evidence.json`, `/tmp/m140_facts.json`, `/tmp/m140_issues.json`, `/tmp/m140_laws.json`, `/tmp/m140_arguments.json`) show domain-relevant items (e.g., 转账凭证/电子回单, 微信聊天记录, 催收记录, 相关法条 citations, legal issues). No obvious mock/demo copy in these records.
- Facts, Issues, Laws, Arguments pages returned HTTP 200 and their HTML saved under `/tmp/m140_*.html`.
 - Evidence page: frontend UI rendering verified. The backend API returns JSON (see `/tmp/m140_evidence.json`) and the page now renders without the prior selected-evidence null crash.

3) Documents
- API: `/matters/m-mrco9m97-dbygsz/documents` returned 3 items (titles: `诉讼策略建议`, `证据清单`, `起诉状`) — all `status: draft`, `content: null`, `content_uri: ""`.
- Frontend Documents page rendered (HTTP 200) and lists the three drafts; selecting a document shows placeholders for missing content (UI uses `—` or empty placeholders). These empty drafts are a UX risk for demo viewers.

4) Mock/demo/sample copy
- This matter's records are generated by AI intake and contain realistic legal language in metadata and field titles. There are no `mock-` or obvious demo/sample text items inside this matter's resource payloads. The global matters list contains many `mock-*` matters, which may clutter selection if using the global list for demos.

5) UI blanks / obvious exceptions
- Evidence workspace: previously caused a server-side rendering error due to null-value access; this was fixed in M141 by adding a minimal guard in `apps/frontend/src/app/matters/[matter_id]/evidence/page.tsx`. Runtime verification shows the page returns 200 and no longer blocks the demo.
- Documents workspace: renders, but empty `content` fields display placeholders. Recommend hiding/archiving or supplying content for demo.

Artifacts (local captures)
- API JSON snapshots: `/tmp/m140_matter.json`, `/tmp/m140_documents.json`, `/tmp/m140_evidence.json`, `/tmp/m140_facts.json`, `/tmp/m140_issues.json`, `/tmp/m140_laws.json`, `/tmp/m140_arguments.json`.
- Frontend page HTML + headers: `/tmp/m140_matters.html`, `/tmp/m140_overview.html`, `/tmp/m140_documents.html`, `/tmp/m140_evidence.html` (500 page), `/tmp/m140_facts.html`, `/tmp/m140_issues.html`, `/tmp/m140_laws.html`, `/tmp/m140_arguments.html`, and corresponding `*_headers.txt` files.

Recommendations (actionable, non-destructive)
- Blocker (high): Fix Evidence page render crash. Short-term patch: guard access to `selectedEvidence` (render conditionally) to avoid server-side TypeError. This is a small frontend fix — after applying, rebuild and verify Evidence page renders.
- UX (medium): For demo sessions, either:
  - Soft-archive the 3 empty draft documents (non-destructive) so they are hidden in the Documents workspace, OR
  - Provide demo-ready content for those drafts (fill `content`) before demo, OR
  - Filter UI in demo mode to hide documents with `content IS NULL`.
- Demo selection: avoid using the global matter list directly (it contains many `mock-*` matters). Instead create/mark a dedicated `demo` matter and pin it, or use the direct matter URL for demos.

Risk & Rollback
- No data or schema changes were made during this audit. Any recommended remediation (e.g., soft-archive) should be reversible and require approvals — follow M137.1 plan if proceeding with bulk cleanup.

Next steps (if you want me to continue)
- I can prepare a minimal frontend patch to guard `selectedEvidence` and open a PR for review (small, isolated change). This meets "minimal modification" and mitigates the 500.
- Alternatively, I can implement a demo-mode UI filter that hides empty drafts (also minimal).

Prepared by: automated local audit run


---

End of report.
