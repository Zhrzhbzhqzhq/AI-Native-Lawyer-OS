# M137.1 空文书清理决策（Empty Documents Cleanup Decision）

日期：2026-07-10

背景
- 依据 M136.1–M136.9 的调查与回归验证：历史数据库中存在 908 条“空文书”记录（文档 `content` 为 null/空且 `content_uri` 为空或无实际内容），这些多由早期 AI intake 流程在 `/intake/ai-create-matter` 路径下产生。
- 已在 M136.8 中实施最小修复：在 `apps/backend/src/routes/intakeRoutes.ts` 的 AI intake 文书持久化处添加了 guard，跳过没有实际文本内容的 AI 文档，从而防止将来产生新的空文书。
- M136.9 本地回归验证确认：修复生效，新的 intake 运行不会产生 `content` 与 `content_uri` 均为空的文档。

现状概述（必须项）
- 总量：数据库中共有 908 条空文书（定义：`content` 为 null/空字符串且 `content_uri` 为空或仅为占位符）。
- 目标 Matter（在早期调查中关注的 matter `m-mrco9m97-dbygsz`）中存在 3 条空文书；已判断源头高度可能为 AI intake（见 M136.6 溯源报告）。

目标
- 在不修改代码、不改 schema、不删除旧数据的前提下，制定对历史 908 条空文书的处置决策，权衡合规与产品体验，明确下一步是否进入清理脚本阶段（M137.2）。

评估要点：是否保留目标 matter 的 3 条空文书？
- 建议：暂时保留（不直接删除）。理由：
  - 这些文书可能具备审计价值（可作为 AI 行为/输入的审计线索）。
  - 业务团队/法务可能要求保留原始 AI 产出以便后续人工核查。
  - 已有修复阻止新的空文书产生，立即删除历史数据不是唯一且安全的选项。

对演示/产品的影响
- 当前 frontend 展示逻辑会将空文书以占位或不友好方式显示（早期为 "—"）。这可能影响 demo/演示体验。
- 因为修复已阻止未来新增空文书，演示环节可通过前端短期显示提示（例如“AI 生成草稿—待完善”）来缓解。历史空文书若保留，则可能在演示中出现异常，需要在演示前做筛选或隐藏处理。

清理选项（必须列出 A/B/C）
A. 暂时保留（Recommended for immediate safety）
- 描述：不对历史空文书做任何数据库写入或逻辑变更，保留原样并建立查询/显示白名单控制以避免在演示或 UI 中直接暴露给最终用户。
- 优点：零风险（无数据变更），保留审计证据，快速可执行。
- 缺点：占用存储、可能影响 demo 输出，需要前端或查询层过滤。

B. 软归档（Soft-archive）
- 描述：为符合清理条件的空文书添加标记（如 `status='archived'` 或 `meta->audit->archived=true`），并在 UI/API 层默认隐藏归档条目。
- 优点：不删除数据，减小默认用户视图噪音，可通过撤销操作恢复；保留审计并改善用户体验。
- 缺点：需要写入数据库修改（改变记录状态），需事前评估外键/索引影响；需业务审批。

C. 备份后删除（Backup + delete）
- 描述：先导出匹配条件的数据（完整 row dump +关联记录），将备份存档（离线/对象存储），然后在主库中删除这些历史空文书。
- 优点：彻底清理生产视图，释放空间，避免未来误触。
- 缺点：不可逆（除非从备份恢复），存在法务/合规风险；删除过程需严格验证和运行窗口，且要准备回滚计划与恢复步骤。

推荐方案（Must state recommendation）
- 推荐：B（软归档）优先；若业务/法务强烈要求保留原始历史记录则采取 A（暂时保留）作为短期缓解；C（备份后删除）仅作为最终、经审批并在满足所有前置条件后采取的选项。
- 推荐理由：软归档在安全（不丢失数据）与改善用户体验之间提供最佳折中。相比完全保留，它能让默认 UI/API 更整洁；相比删除它保留了审计与可恢复性。

执行前置条件（Soft-archive 执行前必须满足）
1. 业务（产品）与法务/合规书面批准（对数据保留期限及访问策略达成共识）。
2. 完整数据库备份（point-in-time 或导出）：在维护窗口前完成，并验证备份可用性（试恢复至测试环境）。
3. 明确定义清理条件（精确 SQL 条件），例如：
   - `content IS NULL OR content = ''`
   - `content_uri IS NULL OR content_uri = ''`
   - `created_at < '2026-07-01'`（可选：限定时间窗口）
   - `created_by = 'ai-intake' OR metadata->>'source' = 'ai'`（若可用）
4. 先在 staging 环境运行脚本并由 QA 验证结果（确认无级联副作用）。
5. 设计并测试回滚方案（见下文）。
6. 设计监控与审计输出：脚本应记录受影响的 `document_id`、`matter_id`、创建时间、原始 `content`（若有）和处理结果。

风险
- 数据丢失风险（若选择删除）：备份失效或恢复失败将导致不可逆损失。
- 合规/法律风险：某些案件数据可能需保留（审计/保全/诉讼保全）。
- 外部依赖风险：某些文书可能被外部任务或引用（evidence/fact/argument）引用，归档或删除可能导致引用不一致或 UI 报错。
- UI/API 暴露风险：若未在前端/API 层加以过滤，用户或 demo 仍会看到归档或空数据。

缓解措施
- 强制备份并验证恢复；在 staging 先演练。
- 软归档优先，避免直接删除。
- 在归档/删除脚本中先执行 SELECT 并输出 CSV，征得业务确认后再执行写操作。
- 在归档时解除或记录所有外键引用（例如把文档上的 `argument_id`/`evidence_id` 设为 null 或记录原值到 audit 表）以保持一致性（需与 DB/团队讨论）。

回滚方式
- 对于软归档：将 `status` 或 `meta` 标记恢复到原状态（简单 SQL UPDATE 反向脚本）。
- 对于删除：从备份恢复（全库或按导出数据恢复），或从导出文件逐条重建记录；必须在执行前测试恢复流程并确认恢复窗口/影响。

是否进入 M137.2 cleanup script？
- 建议：是，但条件化（`yes, conditional`）。
  - 先在 staging 完成软归档脚本开发与验证并完成审批流程后，进入 `M137.2`，实施软归档脚本（并同时提供可选的备份+删除脚本作为后续步骤）。

实施建议（高层步骤）
1. 业务/法务审批（决策：B 或 A 或 C）。
2. 备份并验证（DB dump + sample restore）。
3. 在 staging 上运行软归档脚本，产出受影响记录清单并交付 QA/业务审核。
4. 若审核通过，在生产维护窗口执行软归档脚本并记录变更到 audit 日志表。
5. 将前端/API 层默认过滤/隐藏方式部署到生产，避免展示归档条目。
6. 若最终决定做彻底删除，先执行离线备份并在独立步骤中执行删除（仅在所有团队批准并验证回滚后进行）。

审批人（建议）
- 产品负责人
- 法务/合规代表
- 数据/DBA 负责人
- 后端 owner

附录：建议的 SQL 匹配条件样例（仅示意，需在 staging 验证）

SELECT * FROM document WHERE (content IS NULL OR content = '') AND (content_uri IS NULL OR TRIM(content_uri) = '');

（可加时间或 source 过滤以缩小范围）

结论
- 当前优先级：执行“软归档”方案（B），在完成备份与审批后进入 `M137.2`，先在 staging 实施并验证，再在生产执行。
- 若业务/法务明确要求保留全部原始数据或反对写入任何变更，则采用 A（暂时保留）直到得到进一步指示。

文档作者：自动化运行（建议由负责人签署并推进审批流程）


